{
  "name": "concurrent-task",
  "version": "1.0.0",
  "description": "A concurrent task class which will take an array of promises and a task count which denotes number of tasks to execute at a time",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Saravanan A R",
  "license": "ISC"
}
